export declare const Const: {
    /**
     * ios: codegen generate struct number fields with default value zero.
     *      in some cases, we should check it is invalid value instead of checking is zero.
     */
    NULL_NUMBER: number;
    DEFAULT_ANIM_DURATION: number;
    DEFAULT_EASING: "EaseOut";
    DEFAULT_ZOOM: number;
    DEFAULT_TILT: number;
    DEFAULT_BEARING: number;
    MIN_ZOOM: number;
    MAX_ZOOM: number;
    DEFAULT_SCREEN_DISTANCE: number;
};
//# sourceMappingURL=Const.d.ts.map